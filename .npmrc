registry=https://registry.npmjs.org/
auto-install-peers=false

# Make pnpm record optional native packages for both the local x64 runner and
# the arm64 target platform. This is normal lockfile metadata, but rules_js then
# selects the native optional package using the target platform even for tools.
supported-architectures.os=linux
supported-architectures.cpu=x64,arm64
